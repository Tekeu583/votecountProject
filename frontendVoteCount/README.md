pour lancer le frontend:

* lancer: npm i
* npm run dev

on utilisera lucide-icon pour  les icons sur le frontend

tailwindcss est utiliser pour le style

bref tout les dependances installer sont dans package.json
